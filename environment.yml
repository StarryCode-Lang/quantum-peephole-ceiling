name: q-research
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pip
  # Single source of truth for package pins is requirements.txt.
  # The previous duplicated pin list was removed on 2026-08-06 to
  # eliminate drift risk between this file and requirements.txt.
  - pip:
      - -r requirements.txt
